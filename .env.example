# NRDSandbox Environment Configuration Example
# Copy this file to .env and update with your actual values

# Production Database (DreamHost)
DB_HOST=mysql.newretrodawn.dev
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password
DB_DATABASE=your_db_name
DB_PORT=3306

# Security settings
SESSION_LIFETIME=7200
ENVIRONMENT=production

# Instructions:
# 1. Copy this file to .env
# 2. Update DB_PASSWORD with your actual database password
# 3. Update other values if needed
# 4. Never commit the .env file to git